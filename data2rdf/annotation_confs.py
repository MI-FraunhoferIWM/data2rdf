# The csv2rdf conversion requires a set of relations and classes to use for
# the annotation these are defined separately, so that they can be easily
# adapted.

annotations = {
    "qudt_uri": "http://qudt.org/2.1/vocab/unit",
}
